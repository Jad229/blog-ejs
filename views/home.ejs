<%- include("partials/header"); -%>

<h1>Home</h1>
<p> <%= homeContent %> </p>

<% newPosts.forEach(function(newPost){ %>
    <h2> <%= (newPost.title); %> </h2>
    <p> <%= (newPost.body.substring(0, 100)); %> ...
        <span><a href="/posts/<%= (newPost.title); %>">Read More</a></span>
    </p>
<% }) %>

<%- include("partials/footer"); -%>